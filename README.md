
  # Redesign Admin UI

  This is a code bundle for Redesign Admin UI. The original project is available at https://www.figma.com/design/zvqhUDDHkElyGafAayn1s8/Redesign-Admin-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  